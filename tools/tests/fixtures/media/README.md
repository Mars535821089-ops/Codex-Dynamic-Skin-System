# Deterministic media fixtures

These tiny fixtures are generated by `tools/generate-test-media.sh` from synthetic color bars and sine tones.
They contain no user media. `SHA256SUMS` records the exact committed bytes; regenerate only when intentionally
updating the parser corpus or ffmpeg generation contract.
