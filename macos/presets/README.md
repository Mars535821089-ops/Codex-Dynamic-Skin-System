# Bundled preset

The public repository contains one redistributable showcase preset: `preset-gothic-void-crusade`.

Each preset directory contains `theme.json` and exactly one referenced media file. Imported themes are validated before publication. Only distribute media you created or have permission to redistribute.
